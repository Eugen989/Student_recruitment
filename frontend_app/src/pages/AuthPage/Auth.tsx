import {useTitle} from "../../shared/hooks/useTitle.tsx";

export const Auth = () => {
    useTitle("Auth");
    return (
        <>
            <div>
                <h1>Auth</h1>
            </div>
        </>
    );
};